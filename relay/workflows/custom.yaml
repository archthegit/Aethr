workflow: custom

roles:
  worker: Complete the requested workflow step clearly and concisely.

models:
  worker: openai:gpt-5.5

steps:
  - id: work
    role: worker

review_loop:
  enabled: false
  max_iterations: 1
